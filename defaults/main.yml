---
# defaults file for logstash-role
  logstash_version: "7.10.1"
  logstash_home: "/opt/logstash/{{ logstash_version }}"

  
  #["http://{{ hostvars['elk_elasticsearch_1']['ansible_default_ipv4']['address'] }}:9200"]


    # IP-addresses
  es_host_ip: "172.21.0.2"
  kibana_host_ip: "172.21.0.3"
  logstash_host_ip: "172.21.0.4"